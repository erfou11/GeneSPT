--- a/mapping_utils.py
+++ b/mapping_utils.py
@@ -37,8 +37,8 @@
     """
 
     # remove all-zero-valued genes
-    sc.pp.filter_genes(adata_sc, min_cells=1)
-    sc.pp.filter_genes(adata_sp, min_cells=1)
+    sc.pp.filter_genes(adata_sc, min_cells=0)
+    sc.pp.filter_genes(adata_sp, min_cells=0)
 
     if genes is None:
         # Use all genes
