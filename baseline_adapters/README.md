# Baseline Adapters

This directory is reserved for lightweight wrappers that adapt official
baseline implementations to the frozen strict whole-gene splits.

Do not vendor complete third-party repositories, example datasets, checkpoints,
or generated prediction matrices here. Record exact baseline versions and
prediction checksums in the Zenodo package instead.

